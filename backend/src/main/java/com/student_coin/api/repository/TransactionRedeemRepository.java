package com.student_coin.api.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TransactionRedeemRepository extends JpaRepository<TransactionRedeemRepository, Long> {
}
